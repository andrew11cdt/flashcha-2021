import { makeStyles } from "@material-ui/core";
import { blue } from "@material-ui/core/colors";

export const NewFeedStyle = makeStyles(() => ({
    container: {
        background: blue[50],
        height: '100vh',
    }
}))